[package]
name = "sonolil_camera"
version = "0.1.0"
edition = "2021"

[features]
hub = ["sonolil_hub", "sonolil_movement/hub"]

[dependencies]
sonolil_hub = { path = "../_hub", optional = true }
sonolil_movement = { path = "../movement"}
bevy = { workspace = true, features = ["bevy_camera"]}